import {
  createComplaint,
  getComplaintMessages,
  getUserComplaints,
  replyToComplaint,
} from "@shared/api";

export { createComplaint, getComplaintMessages, getUserComplaints, replyToComplaint };
